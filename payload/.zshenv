# ~/.zshenv -- sourced for every shell; keep minimal
export EDITOR="${EDITOR:-vim}"
export PAGER="${PAGER:-less}"
export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
export ZSH_COMPDUMP="$HOME/.cache/zsh/zcompdump"
